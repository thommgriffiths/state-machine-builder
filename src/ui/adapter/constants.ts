/** Constantes de presentación. Nada de esto se persiste en el documento. */
export const NODE_RADIUS = 36;
/** Ancho del anillo alrededor del nodo desde el que se arrastra para conectar. */
export const RING_PAD = 10;
/** Tamaño del cuadro DOM del nodo (círculo + anillo). */
export const NODE_BOX = 2 * (NODE_RADIUS + RING_PAD);
export const ARROW_LENGTH = 13;
export const ARROW_WIDTH = 11;
export const SELF_LOOP_SIZE = 80;
/** Separación de curvatura automática entre transiciones paralelas/antiparalelas. */
export const PARALLEL_SPREAD = 0.3;
